# Nodo - Intra-Platform Arbitrage Bot

Автоматический бот для поиска арбитражных возможностей на Polymarket.

## 🎯 Что делает бот

Ищет ситуации, когда сумма цен всех исходов < 100%:

```
Пример:
  YES = 48¢
  NO  = 50¢
  ─────────
  Сумма = 98¢

  Покупаем оба за 98¢ → Гарантированно получаем $1 = 2¢ профит (2.04%)
```

## 📁 Структура проекта

```
nodo/bot/
├── main.py              # Главный файл, запуск бота
├── config.py            # Конфигурация из .env
├── polymarket_client.py # API клиент для Polymarket
├── arbitrage_scanner.py # Логика поиска арбитража
├── requirements.txt     # Зависимости Python
├── .env.example         # Пример переменных окружения
└── README.md            # Эта документация
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd nodo/bot
pip install -r requirements.txt
```

### 2. Настройка окружения

```bash
# Скопируй .env.example в .env
copy .env.example .env

# Отредактируй .env и добавь свой приватный ключ
```

### 3. Запуск

```bash
# Один скан
python main.py --once

# Непрерывный мониторинг
python main.py

# С кастомным интервалом (60 секунд)
python main.py --interval 60
```

## ⚙️ Конфигурация

Файл `.env`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `POLYMARKET_API_URL` | URL CLOB API | `https://clob.polymarket.com` |
| `PRIVATE_KEY` | Приватный ключ кошелька | - |
| `MIN_PROFIT_THRESHOLD` | Мин. профит для алерта (%) | `0.5` |
| `MAX_POSITION_SIZE` | Макс. размер позиции (USDC) | `1000` |
| `SCAN_INTERVAL` | Интервал сканирования (сек) | `30` |

## 📊 Как работает арбитраж

### Формула

```
Если: YES_price + NO_price < 1.0

Тогда:
  Gross Profit = (1 - total_price) / total_price × 100%
  Net Profit = Gross Profit - (Fee × Payout)
```

### Пример расчёта

```
Market: "Will BTC hit $100K?"
YES = $0.45
NO  = $0.53
Total = $0.98

Investment: $100
├── Buy YES: $100 × (0.45/0.98) = $45.92
└── Buy NO:  $100 × (0.53/0.98) = $54.08

Guaranteed payout: $100 / 0.98 = $102.04
Gross profit: $2.04 (2.04%)
Fee (2%): $2.04
Net profit: $0.00

⚠️ При 2% комиссии нужен спред > 2% для профита!
```

## 🔮 Roadmap

- [x] Polymarket API клиент
- [x] Intra-Platform сканер
- [x] Расчёт профита с комиссиями
- [ ] Автоматическое исполнение сделок
- [ ] Telegram уведомления
- [ ] Multi-outcome markets
- [ ] Cross-Platform арбитраж

## ⚠️ Дисклеймер

Это экспериментальный софт. Используйте на свой страх и риск. Всегда тестируйте на маленьких суммах.

